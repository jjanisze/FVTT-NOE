<section class="tab {{ tab.cssClass }}" data-tab="{{ tab.id }}" data-group="{{ tab.group }}">
    {{!-- Filled after render by scripts/actors/sheet-shell.mjs, which relocates the
          ammo / magazine / explosive / raw-material panels out of the Ekwipunek tab. --}}
    <div class="neuro-zasoby-root">
        <p class="neuro-zasoby-empty">Brak zasobów do wyświetlenia.</p>
    </div>
</section>
